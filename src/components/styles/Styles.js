const promptStyle = { borderRadius: "5%", width: 0.2 };
const headingPromptStyle = { width: 0.8, paddingLeft: 0, marginLeft: "7.5%" };
const composeContent = {
  whiteSpace: "pre-wrap",
  borderRadius: "5%",
  padding: "6%",
  background: "#f1f1f1",
};
const controlDiv = {
  bottom: "50%",
  borderRadius: "5%",
  left: "1%",
};
const controlButtons = {
  height: "40px",
  width: "40px",
};

const buttonSizing = {
  width: "100%",
  maxHeight: "40px",
  maxWidth: "40px",
};

const targetHandleStyling = { background: "#FF9300" };
const sourceHandleStyling = { background: "#00375B" };
export {
  promptStyle,
  headingPromptStyle,
  composeContent,
  controlDiv,
  controlButtons,
  buttonSizing,
  targetHandleStyling,
  sourceHandleStyling,
};
